# PRD — Mommunjai Phase 3: CRM & Base Reactivation

> เวอร์ชัน 3.0 · 2026-07-26 · PM/BA/SA (Angiris) · สถานะ: **Spec — รออนุมัติก่อนสร้าง**
> ต่อยอดจาก [PRD.md](PRD.md) (เฟส 1) + [PRD-PHASE2.md](PRD-PHASE2.md) (เฟส 2)
> HTML: [PRD-PHASE3.html](PRD-PHASE3.html) · **หน้าจอ+โฟลว์:** `crm-flow-65641ac5.html`
> สถาปัตย์: [ARCHITECTURE.md](ARCHITECTURE.md) · ข้อมูล: [DATA-MODEL.md](DATA-MODEL.md) + migration **0003**

## สารบัญ
- [0. ภาพรวม + ทำไมต้องมีเฟส 3](#0-ภาพรวม)
- [1. ข้อเท็จจริง LINE ที่ยืนยันแล้ว (ฐานของสเปกทั้งฉบับ)](#1-ข้อเท็จจริง-line)
- [2. Epic D — Identity Resolution (เปลี่ยนคนนิรนาม → ลูกค้าที่รู้จัก)](#2-epic-d)
- [3. Epic E — Customer 360 (แกน CRM)](#3-epic-e)
- [4. Epic F — Segment → Audience → Campaign](#4-epic-f)
- [5. Epic G — Lifecycle Automation](#5-epic-g)
- [6. Data Model (migration 0003)](#6-data-model)
- [7. API Surface](#7-api-surface)
- [8. หน้าจอทั้งหมดของเฟส 3](#8-หน้าจอ)
- [9. NFR · ความปลอดภัย · PDPA](#9-nfr)
- [10. ต้นทุนข้อความ + Guardrail](#10-ต้นทุน)
- [11. ตัววัดผล (KPI)](#11-kpi)
- [12. Phasing + Definition of Done](#12-phasing)
- [13. Open Questions](#13-open-questions)
- [14. ความเสี่ยง (Red-team)](#14-ความเสี่ยง)

---

## 0. ภาพรวม
เฟส 1–2 สร้าง "ท่อรับ lead ใหม่" ได้แล้ว **เฟส 3 คือการเปลี่ยน LINE OA เดิมที่มีฐานลูกค้าอยู่แล้ว ให้กลายเป็น CRM (ระบบบริหารความสัมพันธ์ลูกค้า) ที่ใช้งานได้จริง**

### แก่นของปัญหา
> **เพื่อนใน LINE OA ทุกคนเป็นแค่รหัสนิรนาม (userId) จนกว่าเขาจะ "แตะ" เราหนึ่งครั้ง**
> เรามีฐาน แต่ไม่รู้ว่าใครเป็นใคร อยู่ช่วงไหน เคยซื้ออะไร → คุยแบบเข้าใจไม่ได้ ยิงตรงกลุ่มไม่ได้

เฟส 3 แก้ 3 ชั้นตามลำดับ:
1. **รู้จักเขา (Identity)** — Epic D
2. **จำเขาได้ (Memory)** — Epic E
3. **คุยกับเขาให้ถูกคน ถูกเวลา (Reach)** — Epic F + G

### สิ่งที่ **ไม่ทำ** ในเฟส 3 (Non-goals — กัน scope creep)
- ❌ **ไม่สร้างหน้าจอแชทของเราเอง** — แอดมินยังใช้ LINE Official Account Manager / แอป LINE OA เหมือนเดิม (เขาคุ้นแล้ว และ LINE ทำได้ดีอยู่แล้ว) เราทำ "ชั้นสมอง" ไม่ใช่ "กล่องแชท"
- ❌ ไม่ทำระบบขาย/ตะกร้า/ชำระเงินในเฟสนี้
- ❌ ไม่ทำ AI ตอบแชทอัตโนมัติแทนคน (ผิดเส้นแดง compliance เรื่องคำแนะนำสุขภาพ)
- ❌ ไม่ดึงประวัติแชทเก่าย้อนหลัง (LINE ไม่มี API ให้ และเป็นข้อมูลอ่อนไหว)

---

## 1. ข้อเท็จจริง LINE
**ยืนยันกับเจ้าของแบรนด์แล้ว (2026-07-26):**
- ✅ **LINE OA มีโล่ Verified** → ปลดล็อกการดึงรายชื่อผู้ติดตาม (followers) และ Audience ได้เต็ม
- ✅ **Webhook จะชี้มาที่ระบบเรา** (`/api/line/webhook`) — เราเป็นเจ้าของ webhook แต่เพียงผู้เดียว ไม่ชนกับ vendor อื่น

| ความสามารถ | ใช้ได้ | หมายเหตุที่มีผลต่อ design |
|---|---|---|
| ดึง userId ผู้ติดตามทั้งหมด (`/v2/bot/followers/ids`) | ✅ (Verified) | แบ่งหน้า (paginate) ด้วย cursor · ต้องทยอยดึงเป็น batch |
| ดึงโปรไฟล์ (`/v2/bot/profile/{userId}`) | ✅ | ได้ **ชื่อ LINE + รูป + ภาษา** เท่านั้น — **ไม่มีเบอร์ ไม่มีอีเมล ไม่มีเพศ/อายุรายคน** |
| สร้าง Audience จาก userId (`/v2/bot/audienceGroup/upload`) | ✅ | เป็น **งานอะซิงก์** ต้องรอสถานะ `READY` ก่อนใช้ · ทยอยส่งเป็นก้อน |
| ยิงเฉพาะกลุ่ม (`/v2/bot/message/narrowcast`) | ✅ | อะซิงก์ → ตามผลด้วย `requestId` |
| อ่านผลแคมเปญ (`/v2/bot/insight/message/event`) | ✅ | ได้ ส่งสำเร็จ / เห็น / กดลิงก์ |
| อ่านโควตาข้อความคงเหลือ (`/v2/bot/message/quota/consumption`) | ✅ | **ใช้ทำแดชบอร์ดต้นทุนจริง ไม่ต้องเดา** |
| สถิติผู้ติดตามรายวัน (`/v2/bot/insight/followers`) | ✅ | ได้ ผู้ติดตาม / ยิงถึงได้ / **จำนวนบล็อก** ← สำคัญมากสำหรับ circuit breaker |
| ประชากรศาสตร์ของฐาน (`/v2/bot/insight/demographic`) | ✅ | ภาพรวมเพศ/อายุ/พื้นที่ **แบบรวม ไม่ระบุตัวตน** · LINE ซ่อนผลถ้าเป้าหมายน้อยเกินไป |
| เปลี่ยน Rich Menu รายคน | ✅ | คนที่ผูกแล้ว vs ยังไม่ผูก เห็นเมนูคนละแบบ |
| LIFF (เว็บเรารันในแอป LINE) | ✅ | **ได้ userId โดยผู้ใช้ไม่ต้องพิมพ์อะไรเลย** |
| **อ่าน/เขียน "แท็กในห้องแชท" ของ OA Manager** | ❌ **ไม่มี API** | → **ระบบแท็กอยู่ฝั่งเรา** (`tag_assignments` มีแล้ว) และผลัก segment เข้า LINE เป็น **Audience** แทน |
| ดึงประวัติแชทย้อนหลัง | ❌ | นับจากวันติดตั้ง webhook เป็นต้นไปเท่านั้น |
| ข้อความ reply (ตอบเมื่อเขาทัก) | ✅ **ไม่นับโควตา = ฟรี** | → ออกแบบให้ "ล่อให้เขาทัก" ดีกว่า "ยิงหาเขา" |
| ข้อความ push / broadcast / narrowcast | ✅ **นับโควตารายคนรายข้อความ = เสียเงิน** | → ต้องมี guardrail (§10) |

---

## 2. Epic D — Identity Resolution
**เป้าหมาย:** เปลี่ยนผู้ติดตามนิรนาม → ลูกค้าที่รู้จัก โดย**เริ่มจากช่องทางที่ไม่เสียค่าข้อความก่อน**

### User Stories
- ในฐานะลูกค้าเก่า ฉันกดเมนูล่างใน LINE แล้วได้แผนเฉพาะตัวเลย **โดยไม่ต้องพิมพ์รหัสอะไร**
- ในฐานะทีม ฉันอยากรู้ว่าจากฐาน N คน เรารู้จักจริงกี่คนแล้ว และเพิ่มขึ้นวันละเท่าไหร่
- ในฐานะลูกค้าที่เคยกรอกฟอร์มบนเว็บ ฉันอยากให้ระบบรู้ว่า "คนใน LINE" กับ "คนที่กรอกฟอร์ม" คือคนเดียวกัน

### D1 · LIFF Connect (P0) — เสาหลัก
- [ ] สร้าง LIFF app ผูกกับ LINE Login channel ของแบรนด์ → หน้า `/liff/connect`
- [ ] `liff.init()` → `liff.getIDToken()` → **ส่ง ID token ไปให้เซิร์ฟเวอร์ตรวจสอบกับ LINE (`/oauth2/v2.1/verify`)**
  - 🔒 **ห้ามเชื่อ `userId` ที่ส่งมาจากเบราว์เซอร์ตรง ๆ** (ปลอมได้) — ต้องยืนยันฝั่งเซิร์ฟเวอร์เสมอ ตามหลัก BFF ของโปรเจกต์
- [ ] เมื่อยืนยันแล้ว → upsert `line_profiles` + สร้าง/ผูก `customers` → เก็บ `source` ว่ามาจากช่องทางไหน (rich menu / broadcast / เว็บ)
- [ ] ถ้าคนนี้เคยกรอกฟอร์มมาก่อน (มี `lead`) → ผูกเข้าด้วยกันอัตโนมัติ แล้วพาไปที่รายงานเดิม ไม่ต้องกรอกซ้ำ
- [ ] ถ้ายังไม่เคย → พาเข้าแบบสอบถาม `/plan` โดย**ข้ามขั้นตอนถามช่องทางติดต่อ** (เรารู้ LINE เขาแล้ว) → ลดแรงเสียดทาน
- [ ] รองรับเปิดนอกแอป LINE ด้วย (fallback เป็นหน้าเว็บปกติ + ให้กรอกรหัสตั๋วแบบเดิม)

### D2 · Rich Menu 2 แบบ (P0) — ฟรี ได้ผลทุกวัน
- [ ] **เมนู A "ยังไม่รู้จัก"** (ค่าเริ่มต้นของทุกคน): ปุ่มเด่น **"เช็คความพร้อมมีลูก ฟรี"** → LIFF
- [ ] **เมนู B "รู้จักแล้ว"** (ผูกอัตโนมัติหลัง connect): "แผน 90 วันของฉัน" · "วันไข่ตกเดือนนี้" · "คุยกับทีมครูก้อย" · "สินค้าที่แนะนำสำหรับฉัน"
- [ ] สลับเมนูรายคนอัตโนมัติเมื่อผูกสำเร็จ / เมื่อ unfollow แล้ว follow ใหม่
- [ ] หน้าจัดการเมนูใน `/crm/settings/line` (อัปโหลดภาพ + ผูก/ถอด + ดูว่าใครใช้เมนูไหน)

### D3 · Webhook ขยาย (P0)
เพิ่มจากเฟส 2 (ซึ่งจับแค่ข้อความที่มีรหัส MJ-XXXXXX):
- [ ] `follow` → สร้าง `line_profiles` (สถานะ `active`) + บันทึก `interactions` + ส่งข้อความต้อนรับ (reply = ฟรี) + ผูกเมนู A
- [ ] `unfollow` → ตั้งสถานะ `blocked` + บันทึกเวลา → **ตัดออกจากทุก segment อัตโนมัติ** (กันยิงทิ้งเปล่า/เสียเงินฟรี)
- [ ] `message` (ทุกข้อความ ไม่ใช่แค่มีรหัส) → บันทึก `interactions` type=`inbound` + อัปเดต `last_active_at` (ไม่เก็บเนื้อความโดยค่าเริ่มต้น — ดู §9)
- [ ] `postback` → รองรับปุ่มใน Flex เช่น "หยุดรับข่าวสาร" / "ตอนนี้ฉันท้องแล้ว" / "ฉันยังพยายามอยู่"
- [ ] คงพฤติกรรมเดิมทั้งหมด (ตรวจ signature, ตอบ 200 เสมอ, จับรหัสตั๋ว)

### D4 · Backfill ฐานเดิม (P0)
- [ ] งานหลังบ้าน (ทำเป็น admin action ก่อน ไม่ต้อง cron): ดึง `followers/ids` ทีละหน้า → สร้าง `line_profiles` สถานะ `anonymous`
- [ ] ทยอยดึงโปรไฟล์ (ชื่อ+รูป) แบบจำกัดอัตรา (rate limit) — ไม่ยิงรวดเดียว
- [ ] ผลลัพธ์: **รู้ขนาดฐานจริง + ตัวหารของ KPI** และมีรายชื่อไว้ทำ Audience
- [ ] ⚠️ `line_profiles` สถานะ `anonymous` **ห้ามผูกกับข้อมูลสุขภาพใด ๆ** จนกว่าเขาจะยินยอมเอง

### D5 · เส้นทาง "ปลุกฐาน" ที่เสียเงิน (P1 — ต้องอนุมัติก่อนยิง)
- [ ] แคมเปญปลุกฐานเป็น **ระลอก (wave)** ไม่ยิงทีเดียวจบ (ดู §10)
- [ ] ข้อความระลอกแรก **ต้องเป็นคำถาม ไม่ใช่การขาย** + มีปุ่ม "ไม่อยากรับเรื่องนี้แล้ว" เสมอ

**Definition of Done (Epic D):** เปิดจากแอป LINE → กดเมนู → ได้แผนเฉพาะตัว โดยไม่พิมพ์อะไรเลย และในหลังบ้านเห็นเขาเป็น "ลูกค้าที่รู้จัก" พร้อมแท็กครบภายใน 5 วินาที

---

## 3. Epic E — Customer 360
**เป้าหมาย:** แอดมินเปิดดูใครสักคนแล้ว **"เข้าใจเขาได้ใน 10 วินาที"** ก่อนพิมพ์ตอบใน LINE

### User Stories
- ในฐานะแอดมิน เมื่อมีคนทักเข้ามา ฉันอยากรู้ทันทีว่าเขาคือใคร อยู่ช่วงไหน เคยได้แผนอะไรไป เคยคุยอะไรค้างไว้
- ในฐานะหัวหน้าทีม ฉันอยากมอบหมายว่าใครดูแลลูกค้าคนไหน และเห็นว่าค้างตอบกี่ราย
- ในฐานะเจ้าของแบรนด์ ฉันอยากรู้ว่าฐานลูกค้าเรา "สุขภาพดี" แค่ไหน

### E1 · Customer 360 (P0)
หน้า `/crm/c/[id]` — 4 บล็อกในหน้าเดียว:
1. **หัวการ์ด:** ชื่อเล่น + ชื่อ LINE + รูป + ช่วงชีวิต + อายุ + คะแนนความพร้อมล่าสุด + แท็กทั้งหมด + สถานะ (ใหม่/กำลังคุย/ปิด) + ผู้รับผิดชอบ
2. **ไทม์ไลน์:** ทุกเหตุการณ์เรียงเวลา — กรอกฟอร์ม · ใช้เครื่องมือไหน · เปิดรายงานกี่ครั้ง · ทักเข้ามา · เราส่งอะไรไป · กดลิงก์อะไร · ซื้ออะไร
3. **โน้ตทีม:** บันทึกภายใน (ใครเขียน เมื่อไหร่) — **ไม่แสดงให้ลูกค้าเห็น**
4. **แผงลงมือ:** ปุ่ม "เปิดแชทคนนี้ใน LINE" · เพิ่ม/ลบแท็กมือ · เปลี่ยนสถานะ · มอบหมายคน · ส่งรายงานซ้ำ · **ดาวน์โหลด/ลบข้อมูลตามคำขอเจ้าของข้อมูล (PDPA)**

### E2 · รายชื่อลูกค้า (P0)
- [ ] ยกระดับ `/leads` เดิม → `/crm/customers` (คง `/leads` เป็น redirect เพื่อไม่ทำลิงก์เก่าพัง)
- [ ] เพิ่มตัวกรอง: สถานะการรู้จัก (นิรนาม/รู้จัก/บล็อก) · แท็ก · ผู้รับผิดชอบ · ใช้งานล่าสุด · เคยซื้อ/ไม่เคย
- [ ] เลือกหลายรายการ → เพิ่มแท็ก / มอบหมาย / **สร้าง segment จากผลค้นหานี้** (สะพานไปยัง Epic F)

### E3 · แดชบอร์ดสุขภาพฐาน (P0)
หน้า `/crm` — ตัวเลขที่เจ้าของแบรนด์ควรเห็นทุกเช้า:
- ผู้ติดตามทั้งหมด · **รู้จักแล้วกี่คน (%)** · เพิ่มใหม่ / บล็อก (7 & 30 วัน) · **โควตาข้อความเดือนนี้ใช้ไปเท่าไหร่**
- กราฟ "อัตราการรู้จัก" รายสัปดาห์ (ตัวชี้วัดหลักของเฟส 3)
- สัดส่วนช่วงชีวิตของฐานที่รู้จัก (เตรียมท้อง / มีบุตรยาก / ท้องแล้ว / ให้นม / ชาย)
- ⚠️ กล่องเตือน: "มี X คนค้างตอบเกิน 24 ชม."

**Definition of Done (Epic E):** แอดมินเปิดหน้าลูกค้าหนึ่งคน เห็นครบทุกอย่างที่ต้องใช้ตอบแชท โดยไม่ต้องเปิดหน้าอื่น

---

## 4. Epic F — Segment → Audience → Campaign
**เป้าหมาย:** ยิง 1,000 คนที่ใช่ ดีกว่ายิง 20,000 คนมั่ว — และ **รู้ต้นทุนก่อนกดส่งเสมอ**

### F1 · ตัวสร้างกลุ่ม (P0)
- [ ] เงื่อนไขแบบต่อกันได้ (AND/OR): ช่วงชีวิต · แท็ก · ช่วงอายุ · PCOS · แผน ART · คะแนนความพร้อม · ใช้งานล่าสุด · เคยเปิดรายงานไหม · เคยซื้อไหม · **ยินยอมรับข่าวสารไหม (บังคับ)**
- [ ] แสดง **จำนวนคนที่เข้าเงื่อนไขแบบสด** ขณะปรับเงื่อนไข
- [ ] บันทึกเป็น segment ตั้งชื่อได้ · เลือกได้ว่าเป็นแบบ **สด (dynamic)** หรือ **แช่แข็ง ณ เวลานั้น (snapshot)**
- [ ] 🔒 **ตัดออกอัตโนมัติเสมอ:** คนที่บล็อก · คนที่ไม่ยินยอมรับข่าวสาร · คนที่ชนเพดานความถี่ · คนที่ไม่มี `line_user_id`

### F2 · ส่งกลุ่มเข้า LINE เป็น Audience (P0)
- [ ] ปุ่ม "ส่งเข้า LINE" → สร้าง Audience → เก็บ `audience_group_id` + สถานะ (`IN_PROGRESS` / `READY` / `FAILED`)
- [ ] ทยอยส่งเป็นก้อนตามลิมิตของ LINE + ลองใหม่เมื่อพลาด
- [ ] เมื่อ `READY` → ใช้ได้ทั้งใน**ระบบเรา** (narrowcast) และใน **OA Manager** (ทีมเลือกกลุ่มนี้ตอนทำ broadcast เองได้)
- [ ] ซิงก์ซ้ำได้เมื่อสมาชิกกลุ่มเปลี่ยน (สร้าง audience ใหม่ + เลิกใช้ตัวเก่า)

### F3 · ตัวเขียนแคมเปญ 3 ขั้น (P0)
1. **เลือกกลุ่ม** → เห็นจำนวนคนจริงหลังหักคนที่ถูกตัดออก + **ประมาณการต้นทุน**
2. **เขียนข้อความ** → ข้อความ / Flex การ์ด / รูป · แทรกชื่อเล่นได้ · **ตัวอย่างแสดงเป็นหน้าจอ LINE จริง** · ✅ ตัวตรวจ compliance อัตโนมัติ (คำต้องห้าม: "รักษา", "หายขาด", "การันตี", "ท้องแน่นอน") · **บังคับมีปุ่มหยุดรับข่าวสาร**
3. **ตรวจ & ส่ง** → สรุปทั้งหมด + ต้นทุน + **ส่งทดสอบหาตัวเองก่อนเสมอ** + ตั้งเวลา/แบ่งระลอกได้ → ต้องมีสิทธิ์ `send_campaign` จึงกดได้

### F4 · ผลลัพธ์ (P0)
- [ ] ต่อคนต่อแคมเปญ: ส่งสำเร็จ · เห็น · กดลิงก์ · **บล็อกหลังได้รับ**
- [ ] ผูกกลับเข้าไทม์ไลน์ของลูกค้าแต่ละคน
- [ ] เทียบ 2 ข้อความ (A/B) แบบง่าย: แบ่งกลุ่มครึ่ง-ครึ่ง (P1)

**Definition of Done (Epic F):** สร้างกลุ่ม → เห็นจำนวน+ต้นทุน → ส่งทดสอบ → ส่งจริงเป็นระลอก → เห็นผลรายคน และเห็นในไทม์ไลน์

---

## 5. Epic G — Lifecycle Automation
**เป้าหมาย:** ส่งของที่มีประโยชน์ "ถูกเวลา" โดยไม่ใช้แรงคน — และ**ไม่รบกวน**

### G1 · เครื่องยนต์ (P1)
- [ ] นิยาม journey แบบ: **ทริกเกอร์ → เงื่อนไข → รอ → ส่ง → เงื่อนไขหยุด**
- [ ] ทำงานด้วยงานตามเวลา (cron) รายชั่วโมง · idempotent (ส่งซ้ำไม่ได้)
- [ ] **เงื่อนไขหยุดสากล:** ถ้าเขาทักเข้ามาเอง = หยุด journey ทันที (มนุษย์เข้าคุยแล้ว อย่าให้บอทแทรก)

### G2 · Journey ชุดแรก (P1)
| ชื่อ | ทริกเกอร์ | ส่งอะไร | ทำไม |
|---|---|---|---|
| ยังไม่เปิดรายงาน | กรอกครบแล้ว 48 ชม. แต่ยังไม่เปิด | เตือน 1 ครั้ง + ลิงก์ | รายงานคือคุณค่าหลัก อย่าให้ตกหล่น |
| ครบ 30 / 60 / 90 วัน | ผูก LINE + มีแผน 90 วัน | "เช็คอินแผนของคุณ" + ปรับแผน | ทำให้แผนมีชีวิต ไม่ใช่ไฟล์ตาย |
| หน้าต่างไข่ตก | มีข้อมูลรอบเดือน + **ขอรับเตือน** | เตือนล่วงหน้า 2 วัน | มีประโยชน์จริง ทำให้เขารอข้อความเรา |
| เงียบไป 60 วัน | ไม่มีกิจกรรมเลย | ถามไถ่ 1 ครั้ง + ปุ่ม "หยุดรับข่าวสาร" | ปลุกก่อนหาย ไม่ตื๊อ |
| ชวนคู่ | ผู้หญิงที่ยังไม่มีข้อมูลฝ่ายชาย | การ์ดส่งต่อให้สามี | ปัจจัยฝ่ายชาย ~40% (อ้าง PRD-PHASE2) |

- [ ] ⚠️ **ข้อห้ามเชิงจริยธรรม:** ห้ามส่งอัตโนมัติเกี่ยวกับผลตั้งครรภ์/แท้ง/รอบเดือนที่ตีความว่า "ล้มเหลว" · ห้ามใช้ความกลัวเป็นแรงจูงใจ · ทุก journey ต้องผ่าน red-team (lucifer) ก่อนเปิดใช้
- [ ] คนที่กด "ตอนนี้ฉันท้องแล้ว" → **ย้ายออกจาก journey เตรียมท้องทั้งหมดทันที**

---

## 6. Data Model
**Migration `0003_phase3.sql`** — เพิ่มอย่างเดียว ไม่ลบ/ไม่แก้ของเดิม (ตารางเดิมทั้งหมดของเฟส 1–2 ใช้ต่อ)

| ตาราง | หน้าที่ | ฟิลด์สำคัญ |
|---|---|---|
| `customers` | **แกนตัวตน** 1 คน = 1 แถว (คนหนึ่งกรอกฟอร์มได้หลายรอบ = หลาย `leads` แต่เป็นคนเดียว) | `id` · `display_name` · `primary_lead_id` · `owner_staff_id` · `status`(new/active/nurturing/customer/closed) · `first_seen_at` · `last_active_at` · `lifetime_value` |
| `line_profiles` | ขยายจาก `line_bindings` | `line_user_id`(unique) · `customer_id` · `display_name` · `picture_url` · `language` · `follow_state`(anonymous/active/blocked) · `rich_menu_variant` · `source` · `followed_at` · `unfollowed_at` |
| `interactions` | **ไทม์ไลน์** ทุกเหตุการณ์ | `customer_id` · `kind`(inbound/outbound/tool/report_view/link_click/campaign/purchase/note/system) · `channel` · `ref_id` · `summary` · `meta` jsonb · `occurred_at` |
| `customer_notes` | โน้ตภายในของทีม | `customer_id` · `staff_id` · `body` · `created_at` |
| `segments` | นิยามกลุ่ม | `name` · `mode`(dynamic/snapshot) · `rules` jsonb · `created_by` · `last_count` · `last_counted_at` |
| `segment_members` | สมาชิกของ snapshot | `segment_id` · `customer_id` |
| `line_audiences` | ผูก segment ↔ LINE | `segment_id` · `audience_group_id` · `status` · `size` · `synced_at` · `error` |
| `campaigns` | แคมเปญ | `name` · `segment_id` · `message` jsonb · `status`(draft/scheduled/sending/done/paused/failed) · `waves` · `est_recipients` · `est_cost` · `approved_by` · `sent_at` |
| `campaign_recipients` | ผลรายคน | `campaign_id` · `customer_id` · `state`(queued/sent/failed/skipped) · `skip_reason` · `impressed` · `clicked` · `blocked_after` |
| `automations` + `automation_runs` | journey + ประวัติการทำงาน | `trigger` · `conditions` jsonb · `delay_hours` · `message` jsonb · `active` · `stop_on_inbound` |
| `consents` | **แยกความยินยอมเป็นประเภท** | `customer_id` · `kind`(health_data/marketing/line_message) · `granted` · `policy_version` · `source` · `created_at` |
| `purchases` (ทางเลือก) | ประวัติสั่งซื้อ ถ้าแบรนด์แชร์ | `customer_id` · `order_ref` · `sku` · `amount` · `ordered_at` |
| `line_quota_log` | บันทึกโควตารายวัน | `date` · `used` · `limit` · `fetched_at` |

**การเชื่อมกับของเดิม:**
- `leads.customer_id` (เพิ่มคอลัมน์) — lead ทุกอันสังกัดลูกค้า 1 คน
- `line_bindings` คงไว้เพื่อความเข้ากันได้ แต่ **`line_profiles` เป็นแหล่งความจริงใหม่** (เขียนทั้งสองตอนผูก)
- `tag_assignments` ย้ายมาอ้าง `customer_id` ได้ด้วย (รองรับทั้งสองระหว่างเปลี่ยนผ่าน)
- RLS ปิดตายทุกตารางเหมือนเดิม — เข้าถึงผ่าน BFF/service role เท่านั้น

**สิทธิ์ใหม่ (RBAC):** `view_crm` · `edit_customer` · `manage_segments` · `send_campaign` · `import_base` · `manage_automations`
> `send_campaign` และ `import_base` **ให้เฉพาะ admin โดยค่าเริ่มต้น** เพราะมีผลกระทบภายนอกและมีค่าใช้จ่าย

---

## 7. API Surface
ทุกเส้นทางผ่าน BFF · ตรวจสิทธิ์ทุกครั้ง · เบราว์เซอร์ไม่แตะ Supabase/LINE โดยตรง

| Endpoint | สิทธิ์ | ทำอะไร |
|---|---|---|
| `POST /api/liff/connect` | สาธารณะ (ยืนยันด้วย ID token) | ตรวจ ID token กับ LINE → upsert profile/customer → คืนสถานะ + เส้นทางถัดไป |
| `POST /api/line/webhook` | ตรวจ signature | เพิ่ม follow/unfollow/postback/message (ขยายจากเฟส 2) |
| `GET /api/crm/overview` | `view_crm` | ตัวเลขแดชบอร์ด + โควตา |
| `GET /api/crm/customers` | `view_crm` | ค้น/กรอง/แบ่งหน้า |
| `GET /api/crm/customers/[id]` | `view_crm` | โปรไฟล์ + ไทม์ไลน์ + โน้ต |
| `PATCH /api/crm/customers/[id]` | `edit_customer` | สถานะ · ผู้รับผิดชอบ · แท็กมือ |
| `POST /api/crm/customers/[id]/notes` | `edit_customer` | เพิ่มโน้ต |
| `POST /api/crm/customers/[id]/dsr` | `manage_users` | ส่งออก/ลบข้อมูลตามคำขอ PDPA |
| `GET·POST /api/crm/segments` | `manage_segments` | รายการ/สร้าง |
| `POST /api/crm/segments/preview` | `manage_segments` | นับจำนวนสด (ไม่บันทึก) |
| `POST /api/crm/segments/[id]/sync` | `manage_segments` | ส่งเข้า LINE เป็น Audience |
| `GET·POST /api/crm/campaigns` | `send_campaign` | รายการ/สร้างร่าง |
| `POST /api/crm/campaigns/[id]/test` | `send_campaign` | ส่งทดสอบหาตัวเอง |
| `POST /api/crm/campaigns/[id]/send` | `send_campaign` | ส่งจริง (ผ่าน guardrail §10) |
| `POST /api/crm/campaigns/[id]/pause` | `send_campaign` | หยุดระลอกที่เหลือ |
| `GET /api/crm/campaigns/[id]/results` | `view_crm` | ผลรายคน + สรุป |
| `POST /api/crm/line/backfill` | `import_base` | ดึงผู้ติดตามทีละหน้า |
| `GET·POST /api/crm/line/richmenu` | `line_admin` | จัดการเมนู |
| `GET /api/crm/line/quota` | `view_crm` | โควตาคงเหลือ |
| `POST /api/cron/automations` | secret header | เครื่องยนต์ journey |

---

## 8. หน้าจอ
> **ดูของจริงพร้อมสีจริงได้ที่** `crm-flow-65641ac5.html` (13 หน้าจอ + 6 โฟลว์)

| # | หน้าจอ | ใคร | สาระ |
|---|---|---|---|
| S1 | `/crm` แดชบอร์ดสุขภาพฐาน | แอดมิน/เจ้าของ | รู้จักแล้วกี่ % · บล็อก · โควตา |
| S2 | `/crm/customers` รายชื่อ | ทีม | กรอง + เลือกหลายรายการ → สร้าง segment |
| S3 | `/crm/c/[id]` **Customer 360** | ทีม | หัวการ์ด + ไทม์ไลน์ + โน้ต + แผงลงมือ |
| S4 | `/crm/segments` รายการกลุ่ม | ทีม | สถานะซิงก์เข้า LINE |
| S5 | `/crm/segments/new` ตัวสร้างกลุ่ม | ทีม | เงื่อนไข + นับสด + ตัวที่ถูกตัดออก |
| S6 | `/crm/campaigns` รายการแคมเปญ | ทีม | ผล + ต้นทุนจริง |
| S7 | `/crm/campaigns/new` ตัวเขียน 3 ขั้น | ทีม | ตัวอย่างหน้าจอ LINE + ตรวจ compliance + ประมาณต้นทุน |
| S8 | `/crm/campaigns/[id]` ผลลัพธ์ | ทีม | ส่ง/เห็น/กด/บล็อก + รายคน |
| S9 | `/crm/automations` | แอดมิน | เปิด-ปิด journey |
| S10 | `/crm/settings/line` | แอดมิน | สถานะ channel · เมนู · โควตา · ดึงฐาน |
| S11 | `/liff/connect` | **ลูกค้า** (มือถือ) | หน้าเชื่อมต่อ 1 จอ ไม่ต้องพิมพ์ |
| S12 | Rich Menu A / B | **ลูกค้า** | เมนูล่างสองแบบ |
| S13 | ข้อความใน LINE | **ลูกค้า** | ต้อนรับ · ปลุกฐาน · เตือนไข่ตก · หยุดรับข่าวสาร |

---

## 9. NFR
### ความปลอดภัย
- 🔒 **ยืนยัน LIFF ID token ฝั่งเซิร์ฟเวอร์เสมอ** — ห้ามเชื่อ userId จากเบราว์เซอร์
- 🔒 LINE access token / channel secret อยู่ฝั่งเซิร์ฟเวอร์เท่านั้น (ไม่มี `NEXT_PUBLIC_`)
- 🔒 ตรวจ `x-line-signature` ทุก webhook (มีแล้ว) · ตอบ 200 เสมอ
- 🔒 ทุกการกระทำที่มีผลภายนอก (ส่งแคมเปญ / ดึงฐาน / ลบข้อมูล) บันทึก `staff_audit` ว่าใครทำ
- 🔒 จำกัดอัตราเรียก API ของเราเอง เพื่อไม่ให้โดน LINE จำกัด

### PDPA — เส้นแดง
- **แยกความยินยอม 3 ประเภท** (`consents.kind`): ข้อมูลสุขภาพ (ม.26 · ต้องชัดแจ้ง) / การตลาด / การรับข้อความ LINE
- ผู้ติดตามเดิม **ยินยอมแค่รับข่าวสารจาก OA** → **ห้ามผูกข้อมูลสุขภาพกับเขาจนกว่าจะกดยินยอมเอง**
- ทุกข้อความการตลาด **ต้องมีทางถอน** (ปุ่ม postback "หยุดรับข่าวสาร") → บันทึกทันที + ตัดออกทุก segment
- **ไม่เก็บเนื้อความแชทโดยค่าเริ่มต้น** — เก็บแค่ "มีข้อความเข้ามาเมื่อไหร่" (เก็บเนื้อความต้องเป็นการตั้งค่าที่เจ้าของแบรนด์เปิดเอง + แจ้งในนโยบาย)
- **นโยบายเก็บข้อมูล:** ไม่มีกิจกรรม 24 เดือน → ทำให้ไม่ระบุตัวตน (anonymize) โดยอัตโนมัติ
- รองรับคำขอเจ้าของข้อมูล (ขอดู / ขอแก้ / ขอลบ) จากหน้า Customer 360
- อัปเดต `/privacy` ให้ครอบคลุมการใช้ข้อมูลเพื่อ CRM + การส่งข้อความการตลาด ✍️ **ต้องให้เจ้าของแบรนด์ตรวจ**

### ประสิทธิภาพ
- LIFF connect → เห็นผลภายใน **2 วินาที** (คนอยู่บนมือถือ 4G)
- Customer 360 โหลด < 1.5 วินาที (ไทม์ไลน์แบ่งหน้า 50 รายการ)
- Backfill / campaign เป็นงานพื้นหลังทั้งหมด — ไม่บล็อกหน้าจอ

---

## 10. ต้นทุน
> **หลักการ:** ทุก push/broadcast = เงินจริง ต่อคน ต่อข้อความ · reply = ฟรี → **ออกแบบให้ "เขาทักมา" มากกว่า "เรายิงไป"**

### Guardrail ที่ต้องเขียนลงโค้ด (ไม่ใช่แค่นโยบาย)
1. **เพดานความถี่รายคน:** ข้อความการตลาดไม่เกิน **4 ครั้ง/เดือน** และเว้นห่างอย่างน้อย **48 ชม.** — คนที่ชนเพดานถูกตัดออกจาก segment อัตโนมัติ (พร้อมเหตุผล `skip_reason`)
2. **เพดานเดือน:** ตั้งงบข้อความรายเดือนได้ + อ่านโควตาจริงจาก LINE → เกิน 80% เตือน, เกิน 100% **ห้ามส่ง**
3. **ส่งเป็นระลอกบังคับ:** ฐาน > 2,000 คน ต้องแบ่งอย่างน้อย 3 ระลอก เว้นระยะ ≥ 6 ชม.
4. **เบรกอัตโนมัติ (circuit breaker):** ถ้าอัตราบล็อกของระลอกใดเกิน **1.5%** → **หยุดระลอกที่เหลือทันที** + แจ้งแอดมิน
   *(ค่าเริ่มต้นที่ผมตั้งเอง — ควรปรับหลังเห็นข้อมูลจริงระลอกแรก ⚠️ ยังไม่ใช่ค่ามาตรฐานอุตสาหกรรมที่อ้างอิงได้)*
5. **ส่งทดสอบก่อนเสมอ** — กดส่งจริงไม่ได้ถ้ายังไม่เคยส่งทดสอบ
6. **ประมาณการต้นทุนต้องแสดงก่อนกด** — จำนวนผู้รับ × จำนวนข้อความ × ราคาต่อข้อความ (ตั้งค่าราคาได้ใน `/crm/settings/line`)

> ⚠️ **ราคาต่อข้อความ**: ต้องกรอกจากแพ็กเกจจริงของ OA ครูก้อย — **ผมไม่ใส่ตัวเลขเดา** ระบบจะให้แอดมินกรอกครั้งเดียวแล้วคำนวณให้

---

## 11. KPI
| ตัวชี้วัด | นิยาม | เป้าเริ่มต้น |
|---|---|---|
| **อัตราการรู้จัก (ตัวหลัก)** | ลูกค้าที่ผูกแล้ว ÷ ผู้ติดตามทั้งหมด | จาก ~0% → **15% ใน 90 วัน** |
| การเชื่อมต่อแบบไม่เสียเงิน | คนที่ผูกผ่าน rich menu/reply (ไม่ใช่ broadcast) | ≥ 60% ของการผูกทั้งหมด |
| อัตราการกรอกจนจบ | คนที่เข้า LIFF แล้วได้รายงาน | ≥ 55% |
| **อัตราบล็อกจากแคมเปญ** | บล็อกภายใน 24 ชม.หลังได้รับ | **< 1%** (เกิน 1.5% = เบรก) |
| ต้นทุนต่อ lead ที่รู้จัก | ค่าข้อความ ÷ จำนวนคนที่ผูกใหม่ | ติดตามเพื่อเทียบกับค่าโฆษณา |
| อัตราตอบกลับของทีม | ตอบภายใน 24 ชม. | ≥ 90% |

---

## 12. Phasing
| ระลอก | ขอบเขต | ค่าข้อความ | Definition of Done |
|---|---|---|---|
| **P0-a** | migration 0003 · webhook ขยาย · LIFF connect · Rich menu A/B | ฿0 | เปิดจากเมนู LINE → ได้แผน โดยไม่พิมพ์อะไร |
| **P0-b** | Customer 360 · รายชื่อ · แดชบอร์ดฐาน · backfill | ฿0 | แอดมินเปิดดูใครก็ได้ เข้าใจใน 10 วินาที |
| **P1-a** | Segment builder · sync Audience | ฿0 | สร้างกลุ่ม → เห็นใน OA Manager |
| **P1-b** | Campaign composer · ส่ง · ผลลัพธ์ · guardrail ครบ | เสียตามจริง | ส่งทดสอบ → ส่งจริงเป็นระลอก → เห็นผลรายคน |
| **P2** | Lifecycle automation 5 journey | เสียตามจริง | เปิด 1 journey แล้ววัดผลได้ |

**ประเมินคร่าว ๆ:** P0 ≈ 1.5–2 สปรินต์ · P1 ≈ 1.5 สปรินต์ · P2 ≈ 1 สปรินต์ *(ประมาณการของผม ไม่ใช่คำมั่น)*

---

## 13. Open Questions
| # | คำถาม | ถามใคร | บล็อกอะไร |
|---|---|---|---|
| 1 | ราคาต่อข้อความ + แพ็กเกจ OA ปัจจุบัน + จำนวนผู้ติดตามจริง | ทีมครูก้อย | ประมาณการต้นทุน (F3) — **ทำ P0 ได้ไปก่อน** |
| 2 | จะแชร์ประวัติสั่งซื้อเข้าระบบไหม (ถ้าใช่ มาจากที่ไหน) | เจ้าของแบรนด์ | ตาราง `purchases` + journey ซื้อซ้ำ |
| 3 | ใครเป็นเจ้าของ LINE Login channel / LIFF app (ต้องอยู่ใต้บัญชีแบรนด์) | เจ้าของแบรนด์ | D1 — **บล็อกจริง** |
| 4 | นโยบายความเป็นส่วนตัวฉบับเต็ม (ใช้ CRM + ส่งการตลาด) ใครอนุมัติ | เจ้าของแบรนด์ + ที่ปรึกษากฎหมาย | ก่อนยิงแคมเปญแรก |
| 5 | ทีมแอดมินกี่คน ต้องแบ่งเจ้าของลูกค้าจริงไหม | ทีมครูก้อย | E1 (ช่องผู้รับผิดชอบ) |
| 6 | เก็บเนื้อความแชทไหม (ค่าเริ่มต้นของผมคือ **ไม่เก็บ**) | เจ้าของแบรนด์ | D3 |

---

## 14. ความเสี่ยง
| # | ความเสี่ยง | ผลถ้าเกิด | วิธีลด |
|---|---|---|---|
| R1 | **ฐานเดิมไม่ได้อยู่ในช่วงเตรียมท้องแล้ว** (ท้องแล้ว/คลอดแล้ว/เลิกพยายาม) | ข้อความทำร้ายจิตใจ + เสียแบรนด์ | ระลอกแรก**ถามก่อนขาย** + ปุ่ม "ตอนนี้ฉันท้องแล้ว" / "หยุดรับข่าวสาร" ในทุกข้อความ |
| R2 | ยิงแรง → ถูกบล็อก **LINE ไม่คืนผู้ติดตาม** | เสียฐานถาวร | ระลอก + เบรกอัตโนมัติ + เพดานความถี่ |
| R3 | ค่าข้อความบานปลาย | งบแบรนด์เสียหาย | เพดานเดือน + ประมาณการก่อนกด + อ่านโควตาจริง |
| R4 | ผูกข้อมูลสุขภาพกับคนที่ไม่เคยยินยอม | **ผิด PDPA ม.26** | แยก consent 3 ประเภท + `anonymous` ห้ามผูกข้อมูลสุขภาพ |
| R5 | คนปลอม userId เข้ามาดูรายงานคนอื่น | ข้อมูลสุขภาพรั่ว | ยืนยัน ID token ฝั่งเซิร์ฟเวอร์ |
| R6 | webhook เดียวต้องรับทุกเหตุการณ์ ถ้าล่ม = ข้อมูลหาย | ไทม์ไลน์ขาด | ตอบ 200 เสมอ + คิวลองใหม่ + LINE ส่งซ้ำให้ระดับหนึ่ง |
| R7 | Automation ส่งข้อความอ่อนไหวผิดจังหวะ | ทำร้ายกลุ่มเปราะบาง | หยุด journey เมื่อเขาทักมา + red-team ทุก journey ก่อนเปิด |

---

## 15. การอ้างอิงข้ามเอกสาร (กฎซิงก์)
> **โค้ดขยับ = สเปกต้องขยับ** — ถ้าแก้โค้ดใด ๆ ที่กระทบข้อกำหนดในเอกสารนี้ ต้องกลับมาแก้ที่นี่ทันทีในรอบเดียวกัน
- โครงสร้างข้อมูลใหม่ → อัปเดต [DATA-MODEL.md](DATA-MODEL.md) + `supabase/migrations/0003_phase3.sql`
- เส้นทาง/สถาปัตย์ใหม่ → อัปเดต [ARCHITECTURE.md](ARCHITECTURE.md)
- หน้าจอ/สี/ส่วนประกอบใหม่ → อัปเดต [DESIGN.md](DESIGN.md)
- ข้อความถึงลูกค้า → ผ่าน [legal-compliance.md](legal-compliance.md) + brand voice ก่อนปล่อยเสมอ
